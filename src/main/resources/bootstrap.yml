spring:
  application:
    name: apiGatewaySvr # 저장소에서 가져올 yml 파일명
  config:
    import:
    - "optional:configserver:http:///conf"
    - "optional:configserver:http:///conf"
#  cloud:    #    config:#      uri:#      - http:///conf
#      - http:///conf#      fail-fast: true # config server 연결     
server:
  port: 8084

     
